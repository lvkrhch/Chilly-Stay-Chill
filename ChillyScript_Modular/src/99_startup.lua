-- Module: 99_startup.lua
-- Startup defaults and loaded notification.

-- Configuration load is intentionally skipped so only protection features start enabled.

applyWalkSpeed()
applyJumpBoost()
notify("Chilly", "Fixed loaded. Toggle GUI: K")
